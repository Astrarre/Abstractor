package io.github.intransientmc.abstracter.util;

public class SUtil {
	// not actually capitalize but idc
	public static String capitalize(String val) {
		return Character.toUpperCase(val.charAt(0)) + val.substring(1);
	}
}
